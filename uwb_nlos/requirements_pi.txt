# ── Raspberry Pi (inference only) ─────────────────────────────
# Install on RPi: pip install -r requirements_pi.txt
numpy>=1.26
scipy>=1.11
scikit-learn>=1.3    # for RF fallback + scaler
onnxruntime>=1.17    # lightweight ONNX runtime, no torch needed
pyserial>=3.5
